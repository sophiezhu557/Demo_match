services:
  - type: web
    name: abc-mentor-matching-demo
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: DEMO_ACCESS_CODE
        sync: false
